// DUDE RT sun shadows (docs/rtx-shadow-roadmap.md R3): stage wrapper so the interaction_rt
// program loads a full pipeline by name; only the fragment stage differs (ray query).
#include "interaction.vert"
